diff --git a/litex/soc/software/bios/linker.ld b/litex/soc/software/bios/linker.ld
index 62f33379f..a6c7d9080 100644
--- a/litex/soc/software/bios/linker.ld
+++ b/litex/soc/software/bios/linker.ld
@@ -38,6 +38,21 @@ SECTIONS
 		_erodata = .;
 	} > rom
 
+	/* C/C++ constructor (init) arrays. startup.c (litex_startup_init) walks
+	   these, so the boundary symbols must exist even when the arrays are empty.
+	   Const pointer tables, kept in rom. */
+	.init_array :
+	{
+		. = ALIGN(8);
+		__preinit_array_start = .;
+		KEEP(*(.preinit_array))
+		__preinit_array_end = .;
+		__init_array_start = .;
+		KEEP(*(SORT_BY_INIT_PRIORITY(.init_array.*)))
+		KEEP(*(.init_array))
+		__init_array_end = .;
+	} > rom
+
 	.commands :
 	{
 		PROVIDE_HIDDEN (__bios_cmd_start = .);
